# Skill: iac-writer

## Objetivo
Scripts de setup do ambiente local via `scripts/setup.mjs` (Node.js, cross-platform). Stack 100% managed — sem Terraform/Pulumi até escalar.

## Referência
Framework v1.0.0 em `shared/schemas/01-framework-arquitetura-ts-fullstack.md`.
