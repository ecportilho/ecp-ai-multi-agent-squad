# Skill — bundle-optimizer

## Objetivo
Minimizar o tamanho dos arquivos JS e CSS do o produto.

## Estratégias (sem bundler)
- Minificar CSS e JS para produção
- Remover console.log() e comentários em produção
- Lazy load de módulos não críticos
- Carregar fontes com `font-display: swap`
